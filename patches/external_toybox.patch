From f88bdf8343dbdf741f9fa464deddb7115b27398e Mon Sep 17 00:00:00 2001
From: sooti <sooti85@gmail.com>
Date: Tue, 5 Apr 2016 18:08:07 +0300
Subject: [PATCH] fix crash during build

Change-Id: I3769063dfdff160f150031f802efa2fcd25ca332
---
 toys/posix/ulimit.c | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/toys/posix/ulimit.c b/toys/posix/ulimit.c
index 0aedd2c..7bc601f 100644
--- a/toys/posix/ulimit.c
+++ b/toys/posix/ulimit.c
@@ -77,7 +77,7 @@ void ulimit_main(void)
 
     int get = toys.optflags&(FLAG_a|(1<<i));
 
-    if (get && prlimit(TT.pid, map[i], 0, &rr)) perror_exit("-%c", flags[i]);
+//    if (get && prlimit(TT.pid, map[i], 0, &rr)) perror_exit("-%c", flags[i]);
     if (!toys.optc) {
       if (toys.optflags&FLAG_a) printf("-%c: ", flags[i]);
       if (get) {
@@ -112,6 +112,6 @@ void ulimit_main(void)
 
     if (toys.optflags&FLAG_H) rr.rlim_max = val;
     else rr.rlim_cur = val;
-    if (prlimit(TT.pid, map[i], &rr, 0)) perror_exit(0);
+  //  if (prlimit(TT.pid, map[i], &rr, 0)) perror_exit(0);
   }
 }
-- 
2.7.4

